import 'package:flutter/material.dart';

class VertiDivider extends StatelessWidget {
  const VertiDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(height: 40, width: 1.2, color: Colors.grey[300]);
  }
}
